'''
Escreva um programa que solicite ao usuário o ano de
nascimento e, utilizando uma função, retorne com a idade do
usuário.
'''
import os
os.system ("cls || clear")


def calcular_idade(nascimento):
    ano_atual = 2024 #necessario definir o ano para realizar a subtração pela data de nascimento
    idade = ano_atual - nascimento
    return idade


nascimento = int(input("Digite o ano em quem vc nasceu: "))

idade = calcular_idade(nascimento)

print(f"A idade do usuario é: {idade} anos!")
